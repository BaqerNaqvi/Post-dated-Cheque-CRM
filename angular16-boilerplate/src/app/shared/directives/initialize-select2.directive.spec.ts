import { InitializeSelect2Directive } from './initialize-select2.directive';

describe('InitializeSelect2Directive', () => {
  it('should create an instance', () => {
    const directive = new InitializeSelect2Directive();
    expect(directive).toBeTruthy();
  });
});
